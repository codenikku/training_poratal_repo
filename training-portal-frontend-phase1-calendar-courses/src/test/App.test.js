
import React from "react";
import { render } from "@testing-library/react";
import App from "../App";

test("renders the Report component", () => {
  render(<App />);
  // You can add assertions here to check if the Report component is rendered correctly
  // For example, you can check if a specific element or text within the Report component is present
});



